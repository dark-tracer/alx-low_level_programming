#include "main.h"

/**
 * _isdigit - verifies if a character is a numeric value
 * @c: int to be checked
 * Return: 1 if c is a digit, 0 otherwise
 */

int _isdigit(int c)

{
	return (c >= '0' && c <= '9');
}
