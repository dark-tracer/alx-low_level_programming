#include "main.h"
#include <stdio.h>

void _puts(char *str);

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
    puts("\"Programming is like building a multilingual puzzle");
    return (0);
}

void _puts(char *str)
{
    /* Implementation of _puts function goes here */
    /* For example: */
    /* printf("%s\n", str); */
}
