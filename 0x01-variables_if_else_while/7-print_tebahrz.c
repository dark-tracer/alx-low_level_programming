#include <stdio.h>
/**
 * main - Prog Entry point
 * Return: Always 0 (Success)
 */

int mai(void)
{
	char c;

	c = 'z';
	while
		(c >= 'a') {
			putchar(c);
			c--;
		}
	putchar('\n');
	return (0);
}

